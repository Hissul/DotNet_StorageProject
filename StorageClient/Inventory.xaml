<Window x:Class="StorageClient.Inventory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StorageClient"
        mc:Ignorable="d"
        Title="Inventory" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Style="{StaticResource windowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="2*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Image Grid.Column="1" Grid.Row="1" Source="Resources/concept.png" Stretch="Fill"/>

        <Button Grid.Column="2" Grid.Row="2" 
                Height="40" Width="100"
                x:Name="backButton" Click="backButton_Click">
            Назад
        </Button>

    </Grid>
</Window>
